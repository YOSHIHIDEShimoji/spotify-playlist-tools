name: listen-log

# 聴取ログ収集（dashboard-design §6.1）。毎時 recently-played を取得し
# data ブランチの listening/*.jsonl へ追記する。読み取り専用（プレイリストは変更しない）。
#
# 頻度: Spotify の recently-played は直近50件しか保持しないため、間隔中に50曲を超えて
# 再生すると超過分は API から失われる（何度ページングしても取れない）。2分/曲の連続再生でも
# 1時間なら最大30曲 < 50 で余裕を持って取りこぼしを防ぐ（3時間だと最大90曲で溢れうる）。
# public リポなので Actions 分は無料・無制限。cron の分は毎時0分（GitHub の scheduled が最も
# 混んで遅延しやすい）を避けて 17 分にしている。

on:
  schedule:
    - cron: '17 * * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  # nightly / site-ops と同一グループにして data ブランチへの並行 push を無くす（レビュー M7）。
  # listen-log は Spotify を変更しないが、data 書き込みの直列化のため共有する。
  group: spotify-serial
  cancel-in-progress: false

env:
  TZ: Asia/Tokyo
  SPOTIPY_CLIENT_ID: ${{ secrets.SPOTIPY_CLIENT_ID }}
  SPOTIPY_CLIENT_SECRET: ${{ secrets.SPOTIPY_CLIENT_SECRET }}
  SPOTIPY_REDIRECT_URI: 'http://127.0.0.1:8000/callback'

jobs:
  poll:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5

      - uses: actions/checkout@v5
        with:
          ref: data
          path: _data

      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: pip

      - run: pip install -r requirements.txt

      - name: Restore Spotify token cache
        run: printf '%s' "$SPOTIFY_TOKEN_CACHE" > .cache-spotify
        env:
          SPOTIFY_TOKEN_CACHE: ${{ secrets.SPOTIFY_TOKEN_CACHE }}

      - name: Poll recently-played
        run: python listen_log.py --data-dir _data/data

      - name: Push if changed
        working-directory: _data
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data
          if git diff --cached --quiet; then
            echo "聴取ログに変更なし"
            exit 0
          fi
          git commit -m "data: 聴取ログ更新"
          for i in 1 2 3; do
            git push && exit 0
            echo "push 競合。rebase して再試行 ($i/3)"
            git pull --rebase origin data
          done
          exit 1
